import React from 'react';

export default function TagNameList({ tagList, setTagList }: { tagList: string[]; setTagList: (tagList: string[]) => void }) {
    return (
        <div style={{ display: "flex", flexWrap: "wrap" }}>
            tagList
        </div>
    );
} 
